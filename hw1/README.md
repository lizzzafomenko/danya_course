# описание файлов к домашке 1

## [`complement.py`](./complement.py)

**Входные параметры:**
*   `--seq` (`-s`): последовательность (днк)

**Вывод файла (в stdout)**
*   reverse-complement последовательность
*   GC-состав входной последовательности

## [`count_kmers.py`](./count_kmers.py)

**Входные параметры:**
*   `--fa` (`-f`): последовательности в дефолтном fasta файле (c '>' перед именем последовательности)
*   `--kmer` (`-k`): длина kmer-а
*   `--out` (`-o`): название выходного файла

**Вывод файла (в файл)**
*   в файле под именем из аргумента --out в json-формате будет лежать ответ (json.dump с indent = 4)

**пачиму сразу не запушилось**

сломали файл через гитхаб и не сделали `pull`, поэтому запушить изменения сразу не получилось. было два варианта - либо `git pull` и дальше `git add .` -> `git commit -m 'smth'` -> `git push`, но так как я точно знаю что изменение было дурацкое, я сделала `git push --force origin main` - так меньше писать (ну конечно же при условии что я заруинила файл сама)

